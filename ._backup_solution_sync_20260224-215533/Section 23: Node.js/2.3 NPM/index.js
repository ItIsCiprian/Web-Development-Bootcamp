// Import the 'sillyName' module using ES6 syntax.
import generateName from "sillyName";

// Generate a random silly name.
var sillyName = generateName();

// Log the generated name to the console.
console.log(`My name is ${sillyName}.`);

// Import the 'superheroes' module using ES6 syntax.
import superheroes from "superheroes";

// Get a random superhero name.
const name = superheroes.random();
// Log the superhero name to the console.
console.log(`I am ${name}!`);
