<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'API Authentication Demo' %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .container {
      background: white;
      border-radius: 16px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      max-width: 1200px;
      width: 100%;
      padding: 40px;
      animation: fadeIn 0.5s ease-in;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    h1 {
      color: #333;
      margin-bottom: 10px;
      font-size: 2rem;
    }

    .subtitle {
      color: #666;
      margin-bottom: 30px;
      font-size: 0.9rem;
    }

    .button-group {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
      margin-bottom: 30px;
      justify-content: center;
    }

    button {
      padding: 12px 24px;
      font-size: 16px;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      color: white;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
    }

    button:active {
      transform: translateY(0);
    }

    #noAuth {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    #basicAuth {
      background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    }

    #apiKey {
      background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }

    #bearerToken {
      background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
    }

    .response-area {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 25px;
      border: 2px solid #e9ecef;
      min-height: 200px;
      max-height: 600px;
      overflow-y: auto;
    }

    .response-area pre {
      margin: 0;
      font-family: 'Courier New', monospace;
      font-size: 14px;
      line-height: 1.6;
      color: #333;
      white-space: pre-wrap;
      word-wrap: break-word;
    }

    .response-area.error {
      background: #fff5f5;
      border-color: #fc8181;
    }

    .response-area.error pre {
      color: #c53030;
    }

    .auth-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 0.75rem;
      font-weight: 600;
      margin-left: 10px;
      text-transform: uppercase;
    }

    .badge-none {
      background: #e0e7ff;
      color: #4338ca;
    }

    .badge-basic {
      background: #fce7f3;
      color: #be185d;
    }

    .badge-apikey {
      background: #dbeafe;
      color: #1e40af;
    }

    .badge-bearer {
      background: #d1fae5;
      color: #065f46;
    }

    @media (max-width: 768px) {
      .container {
        padding: 20px;
      }

      h1 {
        font-size: 1.5rem;
      }

      .button-group {
        flex-direction: column;
      }

      button {
        width: 100%;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>
      <%= title || 'API Authentication Demo' %>
      <% if (typeof authType !== 'undefined') { %>
        <span class="auth-badge badge-<%= authType %>"><%= authType %></span>
      <% } %>
    </h1>
    <p class="subtitle">Click a button below to test different authentication methods</p>

    <div class="button-group">
      <button type="button" id="noAuth" onclick="window.location.href='/noAuth'">No Auth</button>
      <button type="button" id="basicAuth" onclick="window.location.href='/basicAuth'">Basic Auth</button>
      <button type="button" id="apiKey" onclick="window.location.href='/apiKey'">API Key</button>
      <button type="button" id="bearerToken" onclick="window.location.href='/bearerToken'">Bearer Token</button>
    </div>

    <div class="response-area <%= typeof error !== 'undefined' && error ? 'error' : '' %>">
      <pre><%= content %></pre>
    </div>
  </div>
</body>

</html>
